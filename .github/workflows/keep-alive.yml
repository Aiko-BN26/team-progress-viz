name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *' # 5分おき(遅延する可能性あり)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render site
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://team-progress-viz.onrender.com/ || true
          
