spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?user=postgres.utkzyttlkszgpmscvexa&password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.maximum-pool-size=${DATABASE_MAX_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DATABASE_MIN_IDLE:1}
spring.datasource.hikari.idle-timeout=${DATABASE_IDLE_TIMEOUT:300000}
spring.datasource.hikari.max-lifetime=${DATABASE_MAX_LIFETIME:1800000}
spring.datasource.hikari.connection-timeout=${DATABASE_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.leak-detection-threshold=${DATABASE_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.preferQueryMode=simple

spring.jpa.hibernate.ddl-auto=validate
logging.level.org.hibernate=WARN
logging.level.org.springframework.data=WARN
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false
spring.sql.init.mode=never

# Organization sync options
organization.sync.fetch-commit-details=${ORG_SYNC_FETCH_COMMIT_DETAILS:false}
organization.sync.fetch-pull-request-details=${ORG_SYNC_FETCH_PULL_REQUEST_DETAILS:false}

server.servlet.session.cookie.same-site=None
server.servlet.session.cookie.secure=true
server.forward-headers-strategy=framework

# GitHub OAuth設定
github.oauth.client-id=${GITHUB_CLIENT_ID}
github.oauth.client-secret=${GITHUB_CLIENT_SECRET}
github.oauth.authorize-url=https://github.com/login/oauth/authorize
github.oauth.token-url=https://github.com/login/oauth/access_token
github.oauth.user-url=https://api.github.com/user
github.oauth.callback-url=${GITHUB_CALLBACK_URL:https://team-progress-viz.vercel.app/api/auth/github/callback}

# GitHub API設定
github.api.base-url=${GITHUB_API_BASE_URL:https://api.github.com}

# フロントエンド設定
app.frontend.base-uri=${FRONTEND_BASE_URL:https://team-progress-viz.vercel.app}
app.frontend.success-path=${FRONTEND_SUCCESS_PATH:/auth/callback}
app.frontend.error-path=${FRONTEND_ERROR_PATH:/auth/callback}

